@using Admin.DataLayer;
@model IEnumerable<MusteriYazi>

@{
    ViewBag.Title = "Müşteri Yazı Bilgileri";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<button type="button" class="btn btn-block btn-info btn-sm" data-toggle="modal" data-target="#modalSave" style="width:100px" onclick="addClick()">Ekle</button> <br />

<table id="tblDataList" class="table table-bordered table-hover dataTable dtr-inline" role="grid" aria-describedby="dataList_info">
    <thead>
        <tr role="row">
            <th>İsim</th>
            <th>Ünvan</th>
            <th>Müşteri Yazı</th>
            <th>Firma Url</th>
            <th>Sıra</th>
            <th>Aktif</th>
            <th style="width:150px"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (MusteriYazi musteriYazi in Model)
        {
            <tr role="row" class="odd">
                <td>@musteriYazi.Isim</td>
                <td>@musteriYazi.Unvan</td>
                <td>@musteriYazi.MusteriYazi1</td>
                <td>@musteriYazi.FirmaUrl</td>
                <td>@musteriYazi.Sira</td>
                <td>@musteriYazi.Aktif</td>

                <td>

                    <button type="button" class="btn btn-secondary" data-toggle="modal" data-target="#modalSave" onclick="updateClick(@musteriYazi.MusteriYaziId)" title="Güncelle">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-file-text" viewBox="0 0 16 16">
                            <path d="M5 4a.5.5 0 0 0 0 1h6a.5.5 0 0 0 0-1H5zm-.5 2.5A.5.5 0 0 1 5 6h6a.5.5 0 0 1 0 1H5a.5.5 0 0 1-.5-.5zM5 8a.5.5 0 0 0 0 1h6a.5.5 0 0 0 0-1H5zm0 2a.5.5 0 0 0 0 1h3a.5.5 0 0 0 0-1H5z"></path>
                            <path d="M2 2a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V2zm10-1H4a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1V2a1 1 0 0 0-1-1z"></path>
                        </svg>
                    </button>
                    <button type="button" class="btn btn-secondary" data-toggle="modal" data-target="#modalDelete" onclick="deleteClick(@musteriYazi.MusteriYaziId)" title="Sil">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash" viewBox="0 0 16 16">
                            <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0V6z"></path>
                            <path fill-rule="evenodd" d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1v1zM4.118 4L4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4H4.118zM2.5 3V2h11v1h-11z"></path>
                        </svg>
                    </button>
                </td>
            </tr>
                    }
    </tbody>
</table>

@*Müşteri Yazı ekleme&güncelleme*@
<div class="modal fade" id="modalSave" style="display: none;" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <span id="spanBaslik"></span>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div id="divUpdate" class="modal-body">
                <form role="form">
                    @Html.AntiForgeryToken()
                    <div class="card-body">
                        <div class="form-group">
                            <div class="form-group">
                                <label>İsim</label>
                                <input type="text" class="form-control" id="txtIsim" placeholder="İsim">
                            </div>
                            <div class="form-group">
                                <label>Ünvan</label>
                                <input type="text" class="form-control" id="txtUnvan" placeholder="Ünvan">
                            </div>
                            <div class="form-group">
                                <label>Müşteri Yazı</label>
                                <textarea class="form-control" id="txtMusteriYazi" placeholder="Müşteri Yazı"></textarea>
                            </div>
                            <div class="form-group">
                                <label>Resim</label>
                                <input type="file" class="form-control" id="uplImage" accept="image/*">

                                <a href="/" target="_blank" id="aUploadedImage" style="display:none">Yüklenen Resim</a>
                            </div>

                            <div class="form-group">
                                <label>Firma Url</label>
                                <input type="text" class="form-control" id="txtFirmaUrl" placeholder="Firma Url">
                            </div>
                            <div class="form-group">
                                <label>Sıra</label>
                                <input type="number" class="form-control" id="txtSira" placeholder="Sıra">
                            </div>
                            <div class="form-group">
                                <label>Aktif</label><br />
                                <input type="checkbox" class="form-control" id="chkAktif" placeholder="Aktif">
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer justify-content-between">
                <button type="button" class="btn btn-default" data-dismiss="modal" id="btnModalClose">Kapat</button>
                <button type="button" class="btn btn-primary" onclick="modalKaydet()">Kaydet</button>
            </div>
        </div>
    </div>
</div>

@*Müşteri Yazı silme*@
<div class="modal fade" id="modalDelete" style="display: none;" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <span>Kaydı silmek istediğinize emin misiniz?</span>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-footer justify-content-between">
                <button type="button" class="btn btn-default" data-dismiss="modal">Hayır</button>
                <button type="button" class="btn btn-primary" onclick="modalSil()">Evet</button>
            </div>
        </div>
    </div>
</div>

<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.5/jquery.min.js"></script>
<script>
    $(function () {
        $("#tblDataList").dataTable({
            "responsive": true,
            "autoWidth": true,
            "ordering": false,
            "language": {
                "url": "//cdn.datatables.net/plug-ins/1.10.13/i18n/Turkish.json",
            }
        });
    });

    var selectedRowId = 0;

    function addClick() {
        $("#spanBaslik").text("Müşteri Yazı Ekleme");

        $("#txtIsim").val('');
        $("#txtUnvan").val('');
        $("#txtMusteriYazi").val('');
        $("#txtSira").val('');
        $("#txtFirmaUrl").val('');
        $('#chkAktif').iCheck("uncheck");
        $('#aUploadedImage').hide();
    }

    function updateClick(id) {
        $("#spanBaslik").text("Müşteri Yazı Güncelleme");

        selectedRowId = id;

        var model = @Html.Raw(Json.Encode(Model));

        if (id > 0 && model != null && model.length > 0) {
            for (var i = 0; i < model.length; i++) {
                if (model[i].MusteriYaziId == selectedRowId) {
                    var item = model[i];

                    $("#txtIsim").val(item.Isim);
                    $("#txtUnvan").val(item.Unvan);
                    $("#txtMusteriYazi").val(item.MusteriYazi1);
                    $("#txtSira").val(item.Sira);
                    $("#txtFirmaUrl").val(item.FirmaUrl);

                    var aktifText = item.Aktif ? "check" : "uncheck";
                    $('#chkAktif').iCheck(aktifText);

                    if (item.ResimUrl != undefined && item.ResimUrl != null && item.ResimUrl != "") {
                        var ResimUrl = "Upload/CustomerWritingImage/" + item.ResimUrl;
                        $("#aUploadedImage").attr("href", ResimUrl);

                        $('#aUploadedImage').show();
                    }
                    else {
                        $('#aUploadedImage').hide();
                    }
                }
            }
        }
    }

    function deleteClick(id) {
        selectedRowId = id;
    }

    // kaydetme
    function modalKaydet() {
        var formData = new FormData();

        if (!fieldValidationControl("txtUnvan", "Ünvan boş olamaz") || !fieldValidationControl("txtMusteriYazi", "Müşteri yazı boş olamaz")) {
            return;
        }

        formData.append("__RequestVerificationToken", $("input[name=__RequestVerificationToken]").val());
        formData.append("MusteriYaziId", selectedRowId);
        formData.append("Isim", $("#txtIsim").val());
        formData.append("Unvan", $("#txtUnvan").val());
        formData.append("MusteriYazi", $("#txtMusteriYazi").val());
        formData.append("FirmaUrl", $("#txtFirmaUrl").val());
        formData.append("Sira", $("#txtSira").val());
        formData.append("Aktif", $("#chkAktif").val());

        jQuery.each(jQuery('#uplImage')[0].files, function (i, file) {
            formData.append('file-' + i, file);
        });

        $.ajax({
            type: "POST",
            url: "/CustomerWriting/SaveCustomerWriting",
            data: formData,
            dataType: 'json',
            contentType: false,
            processData: false,
            success: function (data) {
                location.reload();
            }
        });
    }

    // silme
    function modalSil() {
        var customerWriting = {};
        customerWriting.MusteriYaziId = selectedRowId;

        $.ajax({
            type: "POST",
            url: "/CustomerWriting/DeleteCustomerWriting",
            data: '{customerWriting:' + JSON.stringify(customerWriting) + '}',
            contentType: "application/json; charset=utf-8",
            dataType: "json",
            success: function (response) {
                location.reload();
            }
        });
    }
</script>

